{
  "name": "real-balance",
  "version": "1.0.0",
  "private": true,
  "description": "Check register that clears itself against what actually posted at the bank.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "start": "python3 -m http.server 8000",
    "snapshot": "node tools/build-snapshot.mjs"
  }
}
